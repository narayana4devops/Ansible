- name: conditions
  hosts: localhost
  become: yes
  tasks:
  - name: check user exists or not
    ansible.builtin.command: id expense
    ignore_errors: true

  - name: say hi
    ansible.builtin.debug:
      msg: "hi, how are you"